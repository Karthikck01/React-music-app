import React from "react";
import { Container } from "react-bootstrap";
const Contact = () => {
  return (
    <Container style={{ height: `${innerHeight - 56}px` }}>
      <div className="display-5 text-center p-3 fw-bold">Contact</div>
    </Container>
  );
};

export default Contact;
